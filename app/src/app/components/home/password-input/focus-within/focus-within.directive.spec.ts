import { FocusWithinDirective } from './focus-within.directive';

describe('FocusWithinDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusWithinDirective();
    expect(directive).toBeTruthy();
  });
});
