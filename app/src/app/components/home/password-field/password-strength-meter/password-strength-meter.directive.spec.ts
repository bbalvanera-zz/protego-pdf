import { PasswordStrengthMeterDirective } from './password-strength-meter.directive';

describe('PasswordStrengthMeterDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordStrengthMeterDirective();
    expect(directive).toBeTruthy();
  });
});
