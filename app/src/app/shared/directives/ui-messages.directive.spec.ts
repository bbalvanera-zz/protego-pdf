import { UIMessagesDirective } from './ui-messages.directive';

describe('UiMessagesDirective', () => {
  it('should create an instance', () => {
    const directive = new UIMessagesDirective();
    expect(directive).toBeTruthy();
  });
});
