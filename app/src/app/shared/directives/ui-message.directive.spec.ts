import { UIMessageDirective } from './ui-message.directive';

describe('UiMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new UIMessageDirective();
    expect(directive).toBeTruthy();
  });
});
